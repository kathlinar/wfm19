camunda.bpm:
  admin-user:
    id: demo
    password: demo
    firstName: Demo
  filter:
    create: All tasks
  database:
    type: h2
# data source config
# via browser http://localhost:8080/h2-console
spring:
  datasource:
    username: sa
    password: sa
    url: jdbc:h2:mem:camunda
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console